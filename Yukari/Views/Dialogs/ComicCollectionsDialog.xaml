<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog x:Class="Yukari.Views.Dialogs.ComicCollectionsDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:converters="using:Yukari.Helpers.UI.Converters"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:local="using:Yukari.Views.Dialogs"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:selectors="using:Yukari.Helpers.UI.Selectors"
               xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
               xmlns:ui="using:CommunityToolkit.WinUI"
               xmlns:vmc="using:Yukari.ViewModels.Components"
               x:Uid="ComicCollectionsDialog"
               Style="{StaticResource DefaultContentDialogStyle}"
               mc:Ignorable="d">
    <ContentDialog.Title>
        <Grid Width="320" ColumnDefinitions="Auto, *" ColumnSpacing="12">
            <PathIcon VerticalAlignment="Center" Data="M8.40137 3.5C8.92008 2.6033 9.8896 2 11 2H17.25C20.4257 2 23 4.57436 23 7.75V20C23 21.1104 22.3967 22.0799 21.5 22.5987V7.75C21.5 5.40279 19.5972 3.5 17.25 3.5H8.40137ZM8 5C6.34315 5 5 6.34315 5 8V25.25C5 25.5266 5.15224 25.7807 5.39611 25.9113C5.63997 26.0418 5.93588 26.0275 6.16603 25.874L12.5 21.6514L18.834 25.874C19.0641 26.0275 19.36 26.0418 19.6039 25.9113C19.8478 25.7807 20 25.5266 20 25.25V8C20 6.34315 18.6569 5 17 5H8ZM6.5 8C6.5 7.17157 7.17157 6.5 8 6.5H17C17.8284 6.5 18.5 7.17157 18.5 8V23.8486L12.916 20.126C12.6641 19.958 12.3359 19.958 12.084 20.126L6.5 23.8486V8Z" />
            <Grid Grid.Column="1" RowDefinitions="Auto, Auto">
                <TextBlock x:Uid="ComicCollectionsHeader" Style="{StaticResource SubtitleTextBlockStyle}" />
                <TextBlock Grid.Row="1"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           Text="{x:Bind ViewModel.ComicTitle}"
                           TextWrapping="NoWrap"
                           ToolTipService.ToolTip="{x:Bind ViewModel.ComicTitle}" />
            </Grid>
        </Grid>
    </ContentDialog.Title>

    <ContentDialog.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <DataTemplate x:Key="CollectionTemplate" x:DataType="vmc:ToggleOptionViewModel">
            <Grid Height="46"
                  Margin="0,0,0,3"
                  Padding="8,0,8,0"
                  HorizontalAlignment="Stretch"
                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  ColumnDefinitions="*, 40"
                  CornerRadius="4">
                <TextBlock VerticalAlignment="Center" Text="{x:Bind DisplayName}" />
                <ToggleSwitch Grid.Column="1"
                              VerticalAlignment="Center"
                              IsOn="{x:Bind IsSelected, Mode=TwoWay}"
                              OffContent=""
                              OnContent="" />
            </Grid>
        </DataTemplate>
    </ContentDialog.Resources>


    <Grid Width="320">
        <InfoBar x:Uid="EmptyComicCollectionsInfoBar"
                 IsClosable="False"
                 IsOpen="True"
                 Visibility="{x:Bind ViewModel.NoCollections, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />
        <ScrollViewer MaxHeight="300" Visibility="{x:Bind ViewModel.NoCollections, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert, Mode=OneWay}">
            <ItemsControl ItemTemplate="{StaticResource CollectionTemplate}" ItemsSource="{x:Bind ViewModel.Collections, Mode=OneWay}" />
        </ScrollViewer>
    </Grid>
</ContentDialog>